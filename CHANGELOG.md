# 更新说明

## 百宝箱

- 新增“百宝箱”工作区，支持网页链接和飞书文档链接拖拽保存。
- 支持手动添加链接、标题和多个标签，链接数据持久化到本机 `UserDefaults`。
- 支持搜索、标签筛选、宫格卡片视图和一键打开网页。
- 支持标签管理：新增、重命名、删除；删除标签后相关链接自动归入“未分类”。
- 支持卡片右上角收藏；收藏链接会在当前标签分类中置顶，再次点击可取消置顶。
- 拖拽解析支持 URL、纯文本和含 URL 的混合文本，并对无效链接显示提示。

## 构建修复

- 将 Swift/Clang 模块缓存移到可写目录，避免受限环境下缓存权限错误。
- 构建脚本输出可执行文件到工作区可写路径，便于本地签名和验证。

## 文件范围

- `Deskday.swift`：百宝箱数据模型、持久化、拖拽接收、标签与卡片界面。
- `build.sh`：构建缓存路径与输出路径调整。
- `CHANGELOG.md`：本次更新范围说明。
